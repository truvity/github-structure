name: Devbox Update

on:
  schedule:
    - cron: "0 1 * * *"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  run:
    uses: truvity/ci-workflows/.github/workflows/devbox-update.yaml@eb317bdb3240d43404665cc943d1c00e8946f733 # v2.18.0
    secrets: inherit
