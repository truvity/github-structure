name: Renovate

on:
  schedule:
    - cron: "0 0 * * *"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  run:
    uses: truvity/ci-workflows/.github/workflows/renovate.yaml@217f2eab96a85bdd2293b3c640b012745ce4a64d # v2.21.1
    secrets: inherit
