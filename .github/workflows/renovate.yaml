name: Renovate

on:
  schedule:
    - cron: "0 0 * * *"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  run:
    uses: truvity/ci-workflows/.github/workflows/renovate.yaml@968147418b5df03a0a2c0aa66ed13cba49718b76 # v2.16.2
    secrets: inherit
