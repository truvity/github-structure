name: Renovate

on:
  schedule:
    - cron: "0 0 * * *"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  run:
    uses: truvity/ci-workflows/.github/workflows/renovate.yaml@367635e4cd8312c449772fe8e43e6a7afe4f3bf9 # v2.19.0
    secrets: inherit
